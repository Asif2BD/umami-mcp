[ATTACH: demo/umami-mcp-demo.mp4 — use the MP4, X transcodes GIFs badly]

=== MAIN TWEET (under 280, safe on any account) ===

I needed a tool that didn't exist, so I built it in a day and gave it away.

I run Umami on 21 sites. Great analytics. No MCP server, so no AI could use it.

Now it has one.

npx -y @asif2bd/umami-mcp

Open source, MIT, runs on your machine.

github.com/Asif2BD/umami-mcp


=== LONGER VERSION (X Premium only, ~293 chars) ===

I needed a tool that didn't exist, so I built it in a day and gave it away.

I run Umami for analytics on 21 sites. Great tool. No MCP server, so no AI could actually use it.

Now it has one.

npx -y @asif2bd/umami-mcp

Open source, MIT, runs on your own machine.

github.com/Asif2BD/umami-mcp


=== OPTIONAL SELF-REPLY (post after the main tweet is up) ===

The community versions I tried first were built for Umami v2.

v3 renamed things. So they don't error — they return a clean response with the wrong data.

That's the failure mode worth worrying about in AI tooling. Not the crash. The confident wrong answer.

cc @umami_software


=== NOTES ===
- X shortens every link to 23 chars via t.co, so the github URL costs 23 regardless of length.
- Tag @umami_software in the REPLY, not the main tweet. Tagging in tweet 1 narrows initial reach.
